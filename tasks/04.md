# Template de Prompt para Criar uma Rota de API

Crie a rota `GET /workout-plans/:id`

## Descrição

Essa rota deve retornar o plano de treino recebido e seus dias. Para os dias do plano de treino, NÃO RETORNE OS `exercises`, mas apenas `exercisesCount`.

## Requisitos Técnicos

N/A

## Autenticação

- Rota protegida.
- O usuário precisa ser dono do plano de treino recebido.

## Request

```ts
interface Body {}
```

```ts
interface Params {
  date: string;
}
```

```ts
interface Query {}
```

## Response

[Descreva o retorno esperado e o status code, por exemplo:]

```ts
interface StatusCode200 {
  id: string;
  name: string;
  workoutDays: Array<{
    id: string;
    weekDay: string;
    name: stirng;
    isRest: boolean;
    coverImageUrl?: string;
    estimatedDurationInSeconds: number;
    exercisesCount: number;
  }>;
}
```

## Regras de Negócio
